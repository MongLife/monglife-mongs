dependencies {
    // modules
    api project(':modules:monglife-mongs-module-feign')
}

tasks.register('copyPrivate', Copy) {
    copy {
        from '../../configs/clients/monglife-mongs-client-manager/'
        include '*.yml'
        into 'src/main/resources'
    }
}