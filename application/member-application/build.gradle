dependencies {
}

tasks.register('copyPrivate', Copy) {
    copy {
        from('../../configs/application/member-application/') {
            include('application.yml')
            rename('application.yml', 'application-member.yml')
        }
        into('src/main/resources')
    }
    copy {
        from('../../configs/application/member-application/')
        include('application.yml')
        into('src/test/resources')
    }
}