dependencies {
    // monglife-security
    implementation 'com.github.monglife.monglife-module-common:monglife-module-common-security:1.0.5'

    // monglife-mqtt
    implementation 'com.github.monglife:monglife-module-mqtt:1.0.1'

    // clients
    implementation project(':clients:monglife-mongs-client-google')
    implementation project(':clients:monglife-mongs-client-manager')

    // domains
    implementation project(':domains:monglife-mongs-domain-device')
    implementation project(':domains:monglife-mongs-domain-member')
}

tasks.register('copyPrivate', Copy) {
    copy {
        from '../../configs/apps/monglife-mongs-app-user-api/'
        include 'application*.yml'
        into 'src/main/resources'
    }
    copy {
        from '../../configs/common/'
        include 'common*.yml'
        into 'src/main/resources'
    }
}