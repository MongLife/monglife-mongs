dependencies {
    implementation project(':modules:play-code-module')
}

tasks.register('copyPrivate', Copy) {
    copy {
        from '../../spring_backend_sub/apps/play-common-app-internal-api/'
        include 'application*.yml'
        into 'src/main/resources'
    }
}