dependencies {
    api 'org.springframework.kafka:spring-kafka'
}

tasks.register('copyPrivate', Copy) {
    copy {
        from '../../spring_backend_sub/modules/play-kafka-module/'
        include 'application-kafka*.yml'
        into 'src/main/resources'
    }
}