dependencies {
    api'org.springframework.boot:spring-boot-starter-data-jpa'
}

tasks.register('copyPrivate', Copy) {
    copy {
        from '../../spring_backend_sub/modules/play-jpa-module/'
        include 'application-jpa*.yml'
        into 'src/main/resources'
    }
}